# Add global indent for logging

Change a global option that controls indentation for MBG package logging

## Usage

``` r
logging_add_indent()
```

## Details

Increases the `MbgLoggingIndent` option, with default of one if not set
